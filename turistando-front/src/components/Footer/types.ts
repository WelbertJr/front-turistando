//import theme from "../../../styles/theme";
//export type Colors = keyof typeof theme.colors;
export interface HeaderProps {
  headerLinkInicio: string | null;
  headerLinkLogin: string | null;
  headerLinkAdmin: string | null;
}
